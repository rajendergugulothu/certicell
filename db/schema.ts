import { sqliteTable, text, integer, uniqueIndex, index } from 'drizzle-orm/sqlite-core';
export const packs = sqliteTable('packs', {id:text('id').primaryKey(), owner:text('owner').notNull(), serial:text('serial').notNull(), customer:text('customer').notNull(), batch:text('batch').notNull(), chemistry:text('chemistry').notNull(), nominal:integer('nominal').notNull(), status:text('status').notNull(), test:text('test'), soh:integer('soh'), grade:text('grade'), created:text('created').notNull()}, t=>[uniqueIndex('packs_owner_serial').on(t.owner,t.serial), index('packs_owner_created').on(t.owner,t.created)]);
export const certificates = sqliteTable('certificates',{id:text('id').primaryKey(),owner:text('owner').notNull(),packId:text('pack_id').notNull().unique().references(()=>packs.id),snapshot:text('snapshot').notNull(),digest:text('digest').notNull(),issued:text('issued').notNull(),revoked:text('revoked')}, t=>[index('certificates_owner').on(t.owner)]);
export const events = sqliteTable('events',{id:text('id').primaryKey(),owner:text('owner').notNull(),packId:text('pack_id').notNull(),message:text('message').notNull(),created:text('created').notNull()},t=>[index('events_owner_created').on(t.owner,t.created)]);
export const reports=sqliteTable('reports',{id:text('id').primaryKey(),owner:text('owner').notNull(),packId:text('pack_id').notNull().references(()=>packs.id),objectKey:text('object_key').notNull().unique(),name:text('name').notNull(),size:integer('size').notNull(),digest:text('digest').notNull(),created:text('created').notNull()},t=>[index('reports_owner_pack').on(t.owner,t.packId)]);
export const accounts=sqliteTable('accounts',{owner:text('owner').primaryKey(),name:text('name').notNull(),company:text('company').notNull().default(''),created:text('created').notNull(),updated:text('updated').notNull()});
