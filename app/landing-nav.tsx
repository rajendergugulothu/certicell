'use client';
import {useState} from 'react';
import {BatteryCharging,ArrowUpRight,Menu} from 'lucide-react';
import {Sheet,SheetTrigger,SheetContent,SheetTitle,SheetDescription} from '@/components/ui/sheet';
const links=[['The platform','/#platform'],['How it works','/#how-it-works'],['Our approach','/#approach'],['Verify a record','/verify']];
export default function LandingNav(){const [open,setOpen]=useState(false);return <header className="landing-header"><a className="landing-brand" href="/" aria-label="CertiCell home"><span><BatteryCharging size={25}/></span>CertiCell<i>.</i></a><nav className="landing-desktop-nav" aria-label="Main navigation">{links.map(([label,href])=><a key={href} href={href}>{label}</a>)}</nav><div className="landing-nav-actions"><a className="landing-signin" href="/signin">Sign in</a><a className="lp-button lp-small" href="/signup">Get started <ArrowUpRight size={17}/></a></div><Sheet open={open} onOpenChange={setOpen}><SheetTrigger asChild><button className="landing-mobile-menu" aria-label="Open navigation"><Menu size={24}/></button></SheetTrigger><SheetContent className="landing-mobile-sheet"><SheetTitle>CertiCell</SheetTitle><SheetDescription>Independent battery intelligence</SheetDescription><nav aria-label="Mobile navigation">{[...links,['Sign in','/signin'],['Create account','/signup'],['Explore demos','/demo']].map(([label,href])=><a key={href} href={href} onClick={()=>setOpen(false)}>{label}<ArrowUpRight size={17}/></a>)}</nav></SheetContent></Sheet></header>}
