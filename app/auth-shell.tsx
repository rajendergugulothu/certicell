import {BatteryCharging,ShieldCheck,FlaskConical,FileCheck2,ArrowUpRight} from 'lucide-react';
import {chatGPTSignInPath,chatGPTSignOutPath,type ChatGPTUser} from './chatgpt-auth';
export function AuthShell({children,signup=false}:{children:React.ReactNode,signup?:boolean}){return <div className="auth-page"><aside className="auth-story"><a href="/" className="brand"><span className="brandmark"><BatteryCharging size={26}/></span>CertiCell.</a><div className="auth-story-content"><p className="eyebrow">INDEPENDENT BATTERY INTELLIGENCE</p><h2>Better evidence.<br/>Clearer decisions.</h2><p>Bring your battery records, laboratory evidence, and assessments together in one workspace.</p><div className="auth-features">{[[FlaskConical,'Capture the evidence','Record test conditions and retain original lab reports.'],[FileCheck2,'Keep a traceable record','Connect each assessment to its measurements and review.'],[ShieldCheck,'Control your data','Your battery records and reports belong to your account.']].map(([Icon,title,body]:any)=><div key={title}><Icon size={21}/><div><strong>{title}</strong><p>{body}</p></div></div>)}</div></div><p className="auth-story-footer">Second-life battery assessments · Pilot platform</p></aside><div className="auth-right"><nav className="auth-nav"><span>{signup?'Already have an account?':'New to CertiCell?'}</span><a href={signup?'/signin':'/signup'}>{signup?'Sign in':'Sign up'}<ArrowUpRight size={15}/></a></nav><main className="auth-card">{children}</main><footer className="auth-footer">CertiCell pilot assessments are not accredited safety certifications.</footer></div></div>}
export function SignInContent({user}:{user:ChatGPTUser|null}){return <><span className="auth-emblem"><ShieldCheck size={27}/></span><p className="eyebrow">YOUR CERTICELL WORKSPACE</p><h1>{user?'You’re signed in.':'Welcome back.'}</h1><p className="auth-intro">{user?'Continue to your battery assessments and lab reports.':'Sign in to manage your batteries, review lab evidence, and access your assessments.'}</p>{user?<><div className="signed-identity"><small>Signed in as</small><strong>{user.email}</strong></div><a className="primary auth-continue" href="/dashboard">Open workspace <ArrowUpRight size={18}/></a><a className="secondary auth-continue" href="/account">Manage account</a><a className="auth-switch" href={chatGPTSignOutPath('/signin')} target="_top">Sign out to use another account</a></>:<><a className="primary auth-continue" href={chatGPTSignInPath('/dashboard')} target="_top"><ShieldCheck size={18}/>Sign in with ChatGPT</a><p className="auth-help">Use your ChatGPT account to verify your identity. You’ll return to CertiCell after signing in.</p><div className="auth-divider"/><p className="auth-crosslink">First time here? <a href="/signup">Create your CertiCell account</a></p></>}<a className="auth-switch" href="/demo">Explore 3 sample accounts</a><p className="auth-access-note">This is a private pilot. Signing in or creating a profile does not grant access to an owner-restricted site.</p></>}
