import {getUser,signInPath} from '@/app/auth-user';
import {AuthShell} from '@/app/auth-shell';
import ProfileForm from '@/app/account/profile-form';
import {ShieldCheck,ArrowRight} from 'lucide-react';
export const dynamic='force-dynamic';
export const metadata={title:'Sign up | CertiCell'};
export default async function Page(){const user=await getUser();return <AuthShell signup>{user?<ProfileForm mode="create"/>:<><span className="auth-emblem"><ShieldCheck size={27}/></span><p className="eyebrow">CREATE YOUR CERTICELL ACCOUNT</p><h1>Start with a verified identity.</h1><p className="auth-intro">Sign in with your identity provider, then add your name and company to complete your CertiCell profile.</p><ol className="signup-steps"><li><span>1</span><div><strong>Verify your identity</strong><p>Continue through your provider’s sign-in flow.</p></div></li><li><span>2</span><div><strong>Set up your profile</strong><p>Add the details you’ll use in CertiCell.</p></div></li></ol><a className="primary auth-continue" href={signInPath('/signup')} target="_top">Continue to sign in <ArrowRight size={18}/></a><p className="auth-help">CertiCell uses your identity provider for authentication. There is no separate CertiCell password to create or remember.</p><div className="auth-divider"/><p className="auth-crosslink">Already registered? <a href="/signin">Sign in</a></p><a className="auth-switch" href="/demo">Explore sample accounts first</a><p className="auth-access-note">Registration is available to people permitted to access this private pilot.</p></>}</AuthShell>}
